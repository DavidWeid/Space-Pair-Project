import React from "react";
import "./style.css";
import { Link } from "react-router-dom";

const LogLink = props => {
  return (
    // <Link to={props.}
    <h1>Let's change everything</h1>
  );
};

export default LogLink;
